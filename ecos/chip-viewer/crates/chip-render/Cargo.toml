[package]
name = "chip-render"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
chip-view-db = { path = "../chip-view-db" }
chipgeom-format = { path = "../chipgeom-format" }
