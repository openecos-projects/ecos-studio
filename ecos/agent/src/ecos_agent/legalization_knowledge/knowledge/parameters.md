<a id="parameter.legalization.replace_lower_pcof"></a>
## parameter.legalization.replace_lower_pcof

**Meaning:** `RePlAce_LOWER_PCOF` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.replace_upper_pcof"></a>
## parameter.legalization.replace_upper_pcof

**Meaning:** `RePlAce_UPPER_PCOF` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.replace_ref_hpwl"></a>
## parameter.legalization.replace_ref_hpwl

**Meaning:** `RePlAce_ref_hpwl` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.replace_skip_energy_flag"></a>
## parameter.legalization.replace_skip_energy_flag

**Meaning:** `RePlAce_skip_energy_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.adjust_nctugr_area_flag"></a>
## parameter.legalization.adjust_nctugr_area_flag

**Meaning:** `adjust_nctugr_area_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.adjust_pin_area_flag"></a>
## parameter.legalization.adjust_pin_area_flag

**Meaning:** `adjust_pin_area_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.adjust_rudy_area_flag"></a>
## parameter.legalization.adjust_rudy_area_flag

**Meaning:** `adjust_rudy_area_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.area_adjust_stop_ratio"></a>
## parameter.legalization.area_adjust_stop_ratio

**Meaning:** `area_adjust_stop_ratio` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.auto_adjust_bins"></a>
## parameter.legalization.auto_adjust_bins

**Meaning:** `auto_adjust_bins` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.aux_input"></a>
## parameter.legalization.aux_input

**Meaning:** `aux_input` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.base_design_name"></a>
## parameter.legalization.base_design_name

**Meaning:** `base_design_name` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.bndry_padding_x"></a>
## parameter.legalization.bndry_padding_x

**Meaning:** `bndry_padding_x` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.bndry_padding_y"></a>
## parameter.legalization.bndry_padding_y

**Meaning:** `bndry_padding_y` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.cell_padding_x"></a>
## parameter.legalization.cell_padding_x

**Meaning:** `cell_padding_x` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.def_input"></a>
## parameter.legalization.def_input

**Meaning:** `def_input` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.density_weight"></a>
## parameter.legalization.density_weight

**Meaning:** `density_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.detailed_place_command"></a>
## parameter.legalization.detailed_place_command

**Meaning:** `detailed_place_command` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.detailed_place_engine"></a>
## parameter.legalization.detailed_place_engine

**Meaning:** `detailed_place_engine` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.detailed_place_flag"></a>
## parameter.legalization.detailed_place_flag

**Meaning:** `detailed_place_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.deterministic_flag"></a>
## parameter.legalization.deterministic_flag

**Meaning:** `deterministic_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.differentiable_timing_obj"></a>
## parameter.legalization.differentiable_timing_obj

**Meaning:** `differentiable_timing_obj` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.dtype"></a>
## parameter.legalization.dtype

**Meaning:** `dtype` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.dump_global_place_solution_flag"></a>
## parameter.legalization.dump_global_place_solution_flag

**Meaning:** `dump_global_place_solution_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.dump_legalize_solution_flag"></a>
## parameter.legalization.dump_legalize_solution_flag

**Meaning:** `dump_legalize_solution_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.enable_fillers"></a>
## parameter.legalization.enable_fillers

**Meaning:** `enable_fillers` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.enable_net_weighting"></a>
## parameter.legalization.enable_net_weighting

**Meaning:** `enable_net_weighting` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.evaluate_pl"></a>
## parameter.legalization.evaluate_pl

**Meaning:** `evaluate_pl` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.gamma"></a>
## parameter.legalization.gamma

**Meaning:** `gamma` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.get_congestion_map"></a>
## parameter.legalization.get_congestion_map

**Meaning:** `get_congestion_map` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.global_place_flag"></a>
## parameter.legalization.global_place_flag

**Meaning:** `global_place_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.global_place_stages"></a>
## parameter.legalization.global_place_stages

**Meaning:** `global_place_stages` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.gp_noise_ratio"></a>
## parameter.legalization.gp_noise_ratio

**Meaning:** `gp_noise_ratio` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.gpu"></a>
## parameter.legalization.gpu

**Meaning:** `gpu` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.gpu_id"></a>
## parameter.legalization.gpu_id

**Meaning:** `gpu_id` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.ignore_net_degree"></a>
## parameter.legalization.ignore_net_degree

**Meaning:** `ignore_net_degree` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.ignore_net_weight"></a>
## parameter.legalization.ignore_net_weight

**Meaning:** `ignore_net_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.init_loc_perc_x"></a>
## parameter.legalization.init_loc_perc_x

**Meaning:** `init_loc_perc_x` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.init_loc_perc_y"></a>
## parameter.legalization.init_loc_perc_y

**Meaning:** `init_loc_perc_y` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.lef_input"></a>
## parameter.legalization.lef_input

**Meaning:** `lef_input` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.legalize_flag"></a>
## parameter.legalization.legalize_flag

**Meaning:** `legalize_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_halo_x"></a>
## parameter.legalization.macro_halo_x

**Meaning:** `macro_halo_x` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_halo_y"></a>
## parameter.legalization.macro_halo_y

**Meaning:** `macro_halo_y` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_overlap_flag"></a>
## parameter.legalization.macro_overlap_flag

**Meaning:** `macro_overlap_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_overlap_mult_weight"></a>
## parameter.legalization.macro_overlap_mult_weight

**Meaning:** `macro_overlap_mult_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_overlap_weight"></a>
## parameter.legalization.macro_overlap_weight

**Meaning:** `macro_overlap_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_pin_halo_x"></a>
## parameter.legalization.macro_pin_halo_x

**Meaning:** `macro_pin_halo_x` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_pin_halo_y"></a>
## parameter.legalization.macro_pin_halo_y

**Meaning:** `macro_pin_halo_y` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.macro_place_flag"></a>
## parameter.legalization.macro_place_flag

**Meaning:** `macro_place_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.max_net_weight"></a>
## parameter.legalization.max_net_weight

**Meaning:** `max_net_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.max_num_area_adjust"></a>
## parameter.legalization.max_num_area_adjust

**Meaning:** `max_num_area_adjust` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.max_pin_opt_adjust_rate"></a>
## parameter.legalization.max_pin_opt_adjust_rate

**Meaning:** `max_pin_opt_adjust_rate` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.max_route_opt_adjust_rate"></a>
## parameter.legalization.max_route_opt_adjust_rate

**Meaning:** `max_route_opt_adjust_rate` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.momentum_decay_factor"></a>
## parameter.legalization.momentum_decay_factor

**Meaning:** `momentum_decay_factor` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.net_weighting_scheme"></a>
## parameter.legalization.net_weighting_scheme

**Meaning:** `net_weighting_scheme` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.node_area_adjust_overflow"></a>
## parameter.legalization.node_area_adjust_overflow

**Meaning:** `node_area_adjust_overflow` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.num_bins_x"></a>
## parameter.legalization.num_bins_x

**Meaning:** `num_bins_x` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.num_bins_y"></a>
## parameter.legalization.num_bins_y

**Meaning:** `num_bins_y` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.num_threads"></a>
## parameter.legalization.num_threads

**Meaning:** `num_threads` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin2pin_accumulate_weight"></a>
## parameter.legalization.pin2pin_accumulate_weight

**Meaning:** `pin2pin_accumulate_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin2pin_max_weight"></a>
## parameter.legalization.pin2pin_max_weight

**Meaning:** `pin2pin_max_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin2pin_min_weight"></a>
## parameter.legalization.pin2pin_min_weight

**Meaning:** `pin2pin_min_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin2pin_net_weighting"></a>
## parameter.legalization.pin2pin_net_weighting

**Meaning:** `pin2pin_net_weighting` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin2pin_weight"></a>
## parameter.legalization.pin2pin_weight

**Meaning:** `pin2pin_weight` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin_area_adjust_stop_ratio"></a>
## parameter.legalization.pin_area_adjust_stop_ratio

**Meaning:** `pin_area_adjust_stop_ratio` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin_density"></a>
## parameter.legalization.pin_density

**Meaning:** `pin_density` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.pin_stretch_ratio"></a>
## parameter.legalization.pin_stretch_ratio

**Meaning:** `pin_stretch_ratio` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.plot_flag"></a>
## parameter.legalization.plot_flag

**Meaning:** `plot_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.random_center_init_flag"></a>
## parameter.legalization.random_center_init_flag

**Meaning:** `random_center_init_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.random_seed"></a>
## parameter.legalization.random_seed

**Meaning:** `random_seed` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.result_dir"></a>
## parameter.legalization.result_dir

**Meaning:** `result_dir` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.risa_weights"></a>
## parameter.legalization.risa_weights

**Meaning:** `risa_weights` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.routability_opt_flag"></a>
## parameter.legalization.routability_opt_flag

**Meaning:** `routability_opt_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.route_area_adjust_stop_ratio"></a>
## parameter.legalization.route_area_adjust_stop_ratio

**Meaning:** `route_area_adjust_stop_ratio` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.route_info_input"></a>
## parameter.legalization.route_info_input

**Meaning:** `route_info_input` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.route_num_bins_x"></a>
## parameter.legalization.route_num_bins_x

**Meaning:** `route_num_bins_x` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.route_num_bins_y"></a>
## parameter.legalization.route_num_bins_y

**Meaning:** `route_num_bins_y` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.route_opt_adjust_exponent"></a>
## parameter.legalization.route_opt_adjust_exponent

**Meaning:** `route_opt_adjust_exponent` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.scale_factor"></a>
## parameter.legalization.scale_factor

**Meaning:** `scale_factor` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.shift_factor"></a>
## parameter.legalization.shift_factor

**Meaning:** `shift_factor` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.sort_nets_by_degree"></a>
## parameter.legalization.sort_nets_by_degree

**Meaning:** `sort_nets_by_degree` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.start_iter"></a>
## parameter.legalization.start_iter

**Meaning:** `start_iter` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.stop_overflow"></a>
## parameter.legalization.stop_overflow

**Meaning:** `stop_overflow` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.target_density"></a>
## parameter.legalization.target_density

**Meaning:** `target_density` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.timing_eval_flag"></a>
## parameter.legalization.timing_eval_flag

**Meaning:** `timing_eval_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.timing_opt_flag"></a>
## parameter.legalization.timing_opt_flag

**Meaning:** `timing_opt_flag` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.two_stage_density_scaler"></a>
## parameter.legalization.two_stage_density_scaler

**Meaning:** `two_stage_density_scaler` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.unit_horizontal_capacity"></a>
## parameter.legalization.unit_horizontal_capacity

**Meaning:** `unit_horizontal_capacity` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.unit_pin_capacity"></a>
## parameter.legalization.unit_pin_capacity

**Meaning:** `unit_pin_capacity` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.unit_vertical_capacity"></a>
## parameter.legalization.unit_vertical_capacity

**Meaning:** `unit_vertical_capacity` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.use_bb"></a>
## parameter.legalization.use_bb

**Meaning:** `use_bb` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.verilog_input"></a>
## parameter.legalization.verilog_input

**Meaning:** `verilog_input` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**

<a id="parameter.legalization.with_sta"></a>
## parameter.legalization.with_sta

**Meaning:** `with_sta` is a committed configuration field for the legalization stage.

**Role:** The stage runner passes the workspace configuration to its underlying tool; this field participates only according to that tool's configuration parser.

**Source evidence:** **dreamplace.runner**, **dreamplace.module**, **ecc.runner**, **ecc.module**, **config.legalization**
