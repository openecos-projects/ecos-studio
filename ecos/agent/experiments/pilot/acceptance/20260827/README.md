# Parameter Acceptance 2026-08-27

The eight candidate workspaces all produce `ecos.terminal_observation.v3` when
checked against their child manifests. The generated JSON manifest records the
native receipt and terminal observation hashes without copying the ignored flow
outputs into Git.

Current classification: **Engineering Incomplete**. Five candidates have a
materialization `before_sha256` mismatch between the config entry and snapshot;
three additionally have stale effective/unit receipt fields. These candidates
must be regenerated by the native producer before the acceptance gate can be
closed. No QoR or research claim is made from this artifact.

Regenerate with:

```bash
PYTHONPATH=src uv run python scripts/build_parameter_acceptance.py \
  --workspace experiments/pilot/results/gate0-20260824-d/gcd/workspace \
  --output experiments/pilot/acceptance/20260827
```
